# Escalation

Escalate when:

- accepted decisions do not become paper trades correctly
- mode-gating behavior is inconsistent
- a live-dispatch path appears reachable
- executor output diverges from accepted decision intent
